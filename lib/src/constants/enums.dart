enum AnswerType {
  shortText,
  multipleChoice,
  contactInfo,
  address,
  phoneNumber,
  longText,
  yesNo,
  singleChoice,
}

enum ImageLayout {
  inBetween,
  right,
  left,
  rightExpanded,
  leftExpanded,
  background
}
